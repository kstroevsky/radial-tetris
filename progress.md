Original prompt: I have an idea about radial Tetris. Can you implement it? Use React. The pieces falling from all 360 degrees, you can move them around the concentric circle.

- Built and deployed the React radial Tetris game as a Cloudflare Pages PWA.
- Added a mobile-first update: a fixed bottom four-action control dock and an initial Easy / Normal / Hard speed selection.
- QA passed: mobile 390 × 844 layout shows every difficulty option and the bottom dock; Easy/Normal/Hard report 1.0988/0.82/0.574-second initial fall intervals; orbit, rotate, and hard-drop controls update live game state; no browser console errors.
- Build passes with `npm run build`; next task is to deploy the verified `dist` output.
- Mobile fidelity fix verified: the prior 1.5× canvas device-pixel-ratio cap was visibly upscaled on high-density Android screens. The board now permits a 2.5× cap while retaining cached static rendering; `getGamePerformanceStats()` exposes `renderScale` as a regression check. A 390 × 844, DPR-3 fixture produced a 925 × 925 (2.5×) board with no console errors; initial static-layer rendering took 23ms and the next draw took 0.2ms.
- Deployed the fidelity fix directly to the existing Pages project. Cloudflare rejected an in-place Direct Upload-to-Git conversion (API error 8000069); moving this same hostname to Git integration requires deleting and recreating the Pages project after explicit approval.
- Researched mobile Tetris and game-control patterns. Decision: replace the mobile-only equal-width strip with split two-thumb clusters, add soft drop and hold-to-repeat, preserve board drag as a secondary gesture, and leave desktop unchanged. See `mobile-controls-analysis.md`.
- Implemented the mobile-only two-thumb controller: hold left/right to orbit, tap rotate, hold soft drop, and tap hard drop. Timers are pointer-specific for simultaneous touches and are cancelled on release, app blur, or backgrounding. Desktop controls are unchanged.
- Final QA passed: at 390 × 844 / DPR 3 the five targets measure 54–115 px tall; desktop dock is hidden on mobile and mobile dock hidden on desktop. A 410 ms left hold moved five sectors and stopped on release; a 310 ms soft-drop hold moved six rings and stopped; rotation fired once; hard drop locked exactly one piece; simultaneous orbit + rotate worked. No browser errors. `npm run build` and the required game-loop test passed.
- Published the verified build to `https://radial-tetris.pages.dev` (deployment `d0ccd452.radial-tetris.pages.dev`). Production serves asset `index-BsIBFU6t.js`; a production 390 × 844 smoke test confirmed the mobile controller, hint, start flow, and no console errors.
- Reworking the mobile interface into a playfield-centered layout: telemetry is above the board, the circle sits lower around screen center, orbit controls are beside the circle, and rotate/nudge/drop form a compact row beneath it.
- Completed the playfield-centered mobile redesign. At 390 × 844 / DPR 3, score is a 67 px top strip, the board frame is 560 px tall, the circle center aligns at y≈419, orbit buttons are 56 × 76 px alongside the circle, and the action row is 58 px tall beneath it. Hold-orbit, rotate, hold-nudge, and hard drop all work with no browser errors; desktop retains its original dock. A 320 × 568 fixture keeps a 246 px circle and every control on screen. Final build and game-loop QA pass.
- Deployed the playfield-centered layout to `https://radial-tetris.pages.dev` (deployment `d924efa3.radial-tetris.pages.dev`). Production serves `index-BleA1AmV.js` / `index-CqOqrZiX.css`; a live 390 × 844 / DPR 3 check confirms score above board, circle center y≈419, side orbit buttons, compact action row, start flow, and zero console errors.
## Progress

- 2026-07-10: Inspected the supplied exponential-map visual reference and defined a 16-sector, 10-ring inward-falling Tetris ruleset.
- 2026-07-10: Scaffolded the React/Vite prototype and installed dependencies.
- 2026-07-10: Added the deterministic radial game engine with seven-bag pieces, orbiting, rotation with kicks, soft/hard drop, collision, ghost landing, ring clears, combos, levels, score, particles, pause, reset, and text-state output support.
- 2026-07-10: Added the responsive React/canvas interface, mathematical polar-grid art direction, HUD, overlays, keyboard/pointer/touch controls, fullscreen, and accessibility labels.
- 2026-07-10: Production build passed. Deterministic engine smoke tests passed for spawn, sector wrapping/orbit, rotation, drop, pause, and complete-ring collapse.
- 2026-07-10: Browser verification is temporarily blocked because the configured in-app browser rejected local preview navigation. `design-qa.md` records the blocking evidence.
- 2026-07-11: User approved the bundled local Playwright client. Headless canvas-only captures can show partial GPU frames; a headed capture confirmed the rendered polar field is complete. Added a no-request favicon to remove the only browser console error before retesting.
- 2026-07-11: The partial-frame artifact also reproduced intermittently in headed capture. Changed the canvas renderer to compose offscreen and atomically present completed frames, preventing capture/render tearing.
- 2026-07-11: Automated canvas readback still caught incomplete GPU tiles after atomic blitting. Forced the render and presentation contexts onto the readback-safe software path and added a one-pixel flush after presentation.
- 2026-07-11: Isolated the issue with an empty-board baseline: the visible canvas retained a live reference to the offscreen source while the next frame mutated it. Switched to alternating presentation buffers so the displayed source stays immutable until replaced.
- 2026-07-11: Alternating buffers exposed that Chromium still retained live GPU source tiles. Presentation now uses a completed pixel snapshot (`getImageData` → `putImageData`), making every visible frame deterministic and capture-safe.
- 2026-07-11: Removed alternating source buffers after the pixel-copy boundary made them unnecessary; this also eliminated buffer-specific context state as a source of alternating incomplete frames.
- 2026-07-11: Whole-page Playwright captures verified the real ready and gameplay layouts are complete. Removed the cosmetic 60fps pulse and switched the scene to event-driven redraws (movement, gravity, clears, resize), eliminating continuous canvas mutation while also reducing CPU/GPU work.
- 2026-07-11: Updated the deterministic `advanceTime` hook to follow the same event-driven rule, avoiding redundant redraws when the scripted clock advances without changing visible state.
- 2026-07-11: Completed browser interaction coverage: pointer orbit, keyboard rotation/nudge/drop, pause/resume, fullscreen/Escape, restart, responsive 390px layout, and console checks all passed. Final headed gameplay canvas and whole-page screenshots were visually inspected.
- 2026-07-11: Compared the reference and gameplay capture in a combined image. Design QA passed with no actionable P0/P1/P2 findings. Final production build and engine smoke test passed.
- 2026-07-11: Added PWA support: standalone manifest, real 192px/512px install icons, production-only service worker, offline app-shell fallback, and persistent local high score.
- 2026-07-11: Completed mobile performance pass. Removed full-canvas readback/copy, cached static geometry, reduced phone pixel density, coalesced pointer moves, made HUD updates event-driven, and removed idle particle allocations. See `performance-review.md` for before/after evidence.

## TODO

- Optional follow-up only: sound design and physical-device battery/thermal profiling.

- Pull-controller follow-up: the GitHub `main` draft replaces the side buttons with left Spin and right Orbit vertical pull tracks (down = clockwise, up = counterclockwise), keeps a centered hold-to-nudge button, and moves New Game/Pause to the bottom. Corrected a CSS specificity regression that had hidden those mobile controls despite their code being present; local mobile, compact-screen, desktop, and game-loop QA pass.
- 2026-07-12: Replaced the mobile Orbit pull track with direct drag around the canvas, restored the canvas to the full available width, and placed diagonal capsule-shaped Spin and Nudge controls at the circle’s lower-left/lower-right edges. Spin’s two poles illuminate for its CW/CCW pull direction; Nudge keeps hold-to-repeat and lights its inward pole.
- 2026-07-12: QA passed at 390 × 844 / DPR 3 and 320 × 568. The 390px viewport now gives the canvas 370px of width; a diagonal CW Spin pull changed rotation, a held Nudge moved the active piece from ring 8 to ring 3, and a canvas drag changed sector from 0 to 10. The removed Orbit control has zero matching elements, desktop is unchanged, and no console errors were observed.
- 2026-07-12: Added tap targets to the Spin capsule’s two poles: top is immediate counter-clockwise rotation and bottom is immediate clockwise rotation. The pull implementation records whether the pointer crossed a rotation step so a drag never also registers as a pole tap.
- 2026-07-12: Pole-tap QA passed at 390 × 844 / DPR 3: top tap changed rotation by −1, bottom tap by +1, and a diagonal drag by +2 without an extra tap rotation. Canvas capture was visually inspected and the browser reported no errors.
- 2026-07-12: Simplified Nudge to a one-ended control, enlarged the polar field from 85% to 90% of its canvas while keeping angular labels in bounds, raised the mobile canvas on normal-height phones, and made both controls slimmer. Added viewport-lock rules for normal desktop/phone sizes; short desktop and phone screens retain scrolling rather than clipping content.
- 2026-07-12: Fullscreen-layout QA passed. At 390 × 844, document scroll height equals viewport height and Nudge’s top-pole count is zero; a 260ms hold moved the active piece from ring 8 to ring 4. At 1280 × 800 and 1280 × 900, scroll height equals viewport height with the decorative footer hidden. At 1280 × 680, content remains scrollable (764px) rather than cropped. Visual inspection confirmed the larger field, legible labels, slimmer controls, and zero browser errors.
- 2026-07-13: Increased the polar field a further 2% of its canvas. Added pointer-down lighting for Spin poles and Nudge, plus a subtle mobile-only bottom orbit rail with holdable left/right arrows as a secondary orbit option.
- 2026-07-13: Orbit-rail QA passed at 390 × 844 / DPR 3: top Spin press exposes the `up` pole state and rotates counter-clockwise on release; a small down-right pull exposes the `down` lighting state; Nudge hold moved ring 8 to ring 5; holding the right rail arrow advanced sector 3 to 11. The rail is hidden at 320 × 568 and on desktop; no browser errors were observed. Visual inspection confirmed stable press lighting after a forced redraw.
- 2026-07-13: Removed mobile frame-corner ornaments that resembled unexplained vertical/horizontal lines. Expanded the field to 95% of its canvas, moved capsules toward the screen edges, extended the orbit rail to meet their centers, rebuilt control labels as stable horizontal content groups, and replaced full-control fills with directional active-end gradients. Normal-height mobile now locks `html`, `body`, and the app shell to the dynamic viewport to prevent browser-page scrolling.
- 2026-07-13: Final mobile-layout QA passed at 390 × 844 / DPR 3: body is fixed and document height equals 844px; frame corner ornament is `display: none`; field diameter is 352px; Spin and Nudge centers are x=72/x=326 and rail arrow centers match x=72/x=326. Spin and Nudge expose the expected directional gradient images, labels are `nowrap` with stable 7px line-height, and there are no browser errors.
- 2026-07-13: Restored browser pull-to-refresh on normal-height mobile by removing the fixed/overflow-hidden root lock while retaining a one-viewport game shell. At 390 × 844 the document still has no scroll range (`scrollHeight === innerHeight`), while the root has `overflow-y: auto` and `overscroll-behavior-y: auto`. Added independent left/right rail glow segments: each active arrow lights the inner 28% of its own rail half with a lime gradient. Local screenshot QA and console checks passed for both directions.
- 2026-07-13: Extended each active orbit-rail gradient from 28% to 60% of its rail half to match the marked mobile reference. At 390 × 844, each segment is 76px and visually fades from the held arrow toward the center; production build, game-loop test, held right-arrow screenshot, and console check pass.
- 2026-07-13: Corrected the rail-reference interpretation: the requested expansion was for touchability, not the glow. Restored the visual glow to its original subtle 28%-of-half-line length. Each arrow is now an invisible 138 × 64px target at 390 × 844, covering about 76px of its adjacent line plus the outside space beyond the arrow. Verified taps at left/right line and outer-edge positions trigger the appropriate orbit action, with no browser errors.
- 2026-07-13: In the local Desktop/dev/radial-tetris main checkout (including the user's unpushed `481d7e4`), replaced the Spin and Nudge linear core fills with soft blue radial ambient halos. Both active Spin poles now use the same yellow circular treatment; their glyphs are one shared 19px icon, mirrored for direction. Local production build and browser-game regression pass; both pressed Spin states were visually inspected at 390 × 844 with zero browser errors.
- 2026-07-13: Restored the Nudge action glyph’s explicit 18px base size after the shared controller-font change made it render as a tiny arrow. Added the linked `2026 | Created and delivered by kstroevsky` footer credit: a compact fixed safe-area credit on phone and a small right-hand footer credit on larger screens.
- 2026-07-13: Added compact tablet behavior for 701–1100px widths: a shallow three-column systems strip, bounded start card, and viewport-height shell on normal-height tablets. Visual QA passed at 390 × 844, 1024 × 768, and 1440 × 900; each has `scrollHeight === innerHeight`, all tablet difficulties are visible, and no browser console errors were found. Production build and the browser-game loop pass.
- 2026-07-13: Increased the mobile copyright from 7px to 8px and kept it visible in both the difficulty and pause overlays. Rebalanced the compact tablet system strip from `120px / flexible / 205px` to `110px / 0.65fr / 1.35fr`, so the integrity block is compact and the three action buttons are 44px tall. Build, 390 × 844 start/pause screenshots, 1024 × 768 no-scroll visual QA, browser console checks, and the gameplay loop all pass.
- 2026-07-14: Reproduced the mobile hold leak when pointer capture fails and release occurs off-target. Reusing a pointer ID can overwrite the only timer reference, leaving an orbit or Nudge interval alive even after blur. Baseline mobile profiling found the normal renderer fast (~0.15ms dynamic draws, no long tasks), while 20 leaked holds drove ~219 redraws/s and ~297ms of main-thread task time per 1.1s sample. Added a browser regression that covers off-target release, pointer reuse, Nudge, normal hold/release, and Spin tap behavior; it fails on the current implementation as expected.
- 2026-07-14: Replaced mobile hold intervals with independently owned, self-cancelling timeout states; added global pointer-end fallback, pointer-ID replacement cleanup, blur/background/mode cleanup, and preserved Spin/pull semantics. The browser regression now passes. Attempting 20 lost releases produces only gravity redraws (~1/s) instead of ~219 redraws/s, with task time dropping from ~297ms to ~12ms per matched sample.
- 2026-07-14: Suspended the game RAF loop when ready, paused, hidden, or game-over without active effects, and stopped React HUD rerenders on particle-only frames. Ready/paused RAF frequency fell from ~120/s to zero; median measured task time fell from ~15.9ms to ~1.0ms ready and ~16.1ms to ~0.37ms paused over 1.5s. Normal sustained mobile controls retain ~0.13ms dynamic draws, ~1.3ms Orbit press handling, ~0.5ms Nudge press handling, stable frame pacing, and zero long tasks/errors.
- 2026-07-14: Reduced frequent input work further with a lightweight HUD-only snapshot, reuse of unchanged ring-integrity data, and a memoized integrity readout. Expanded the mobile regression to cover independent two-finger Orbit + Nudge input and pause/restart cleanup. Unit tests, development and production mobile suites, the Vite production build, production visual QA at 390 × 844, and a simultaneous-control profile all pass; the final production sample averaged 0.123ms per canvas draw with zero long tasks.
- 2026-07-14: Began the serverless leaderboard implementation. Added per-game UUIDs and active-play timing, the D1 player/result schema, profile/result/leaderboard Pages Functions, atomic result aggregation, API-only Functions routing, and a service-worker API bypass. Client UI and end-to-end verification are in progress.
- 2026-07-14: Completed the Pages Functions + D1 leaderboard. First-time players get an editable callsign backed by an anonymous HTTP-only cookie; rankings support max-rings, average-rings, and active-time sorting; completed games submit once with UUID deduplication. Local Pages/D1 integration verified cookie/profile creation, all sort modes, and duplicate idempotency. Unit tests, Functions compilation, production build, mobile controls, and a full browser profile → game-over → leaderboard flow pass with exactly one result request and zero console errors. Mobile and desktop dialogs were visually inspected; the mobile table was tightened so every metric is visible without horizontal scrolling. See `LEADERBOARD_SETUP.md` for binding and deployment steps.
- 2026-07-14: Tightened mobile control responsiveness from user feedback. Nudge remains immediate but now waits 240ms before 96ms repeats, and all held repeats stop when the active piece changes so input cannot spill into the next piece. Spin/Nudge keep their existing artwork inside larger axis-aligned thumb targets; Spin poles expose at least 48px hit regions, pressed controls gain immediate physical scale/brightness feedback, and the staged New Game/Pause pointer-events regression was corrected. Expanded mobile browser coverage verifies short holds, lock redraw, piece-boundary cancellation, hit geometry, and press feedback. Unit tests, production build, the mobile suite, and the required game-loop client pass.
